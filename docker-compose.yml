version: "3.9"

services:
  hacga1:
    build: .
    container_name: hacga1
    privileged: true
    volumes:
      - /test_data:/data
      - ${HOME}/.gm_key:/root/.gm_key:ro
    environment:
      - HOME=/root
    stdin_open: true
    tty: true

